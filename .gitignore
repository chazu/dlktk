# built binary
/dlktk

# local discussion pointer + any local pudl store (the committed record of an
# exercise is its exported .ndjson, not the store)
/.dlktk/
examples/*/.dlktk/
examples/*/.backups/
*.db
*.db-wal
*.db-shm
go.work
go.work.sum
